name: "ghost-sre-env"
description: "SRE investigation and system recovery environment."
version: "1.0.0"
entry_point: "main:app"  # This should point to your FastAPI server file

tasks:
  - id: "easy_cpu_spike"
    difficulty: "easy"
    description: "Kill rogue process causing CPU spike"
  - id: "medium_port_mismatch"
    difficulty: "medium"
    description: "Fix service port mismatch"
  - id: "hard_hidden_config"
    difficulty: "hard"
    description: "Detect and remove hidden configuration file"