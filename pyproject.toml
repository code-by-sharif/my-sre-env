[project]
name = "openenv_project"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "numpy",
    "pydantic"
]

[project.scripts]
server = "server.app:app"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"