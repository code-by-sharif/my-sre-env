[project]
name = "sre_env"
version = "0.1.0"
description = "SRE OpenEnv environment"
authors = [{ name = "Shaik Mastan Vali" }]
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"