[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build-meta"

[project]
name = "sre-env"
version = "0.1.0"
description = "SRE Environment for OpenEnv Hackathon"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv", 
    "pydantic"
]